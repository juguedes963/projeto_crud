var usuario=class Usuario{
    constructor(nome,email,senha,CPF,nick){
        this.CPF=CPF
        this.nome=nome
        this.email=email
        this.senha=senha
    }
    createNickname(nick){
        
        return this.createNickname(nick)
    }
}